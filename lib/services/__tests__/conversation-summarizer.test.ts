/**
 * Conversation summarizer tests — removed.
 *
 * The conversation summarizer was removed as part of the secretary
 * simplification (secretary is a dispatcher, not a chatbot).
 */

import { describe, it } from 'vitest';

describe('conversation-summarizer (removed)', () => {
  it('stub — feature removed', () => {
    // No-op: summarizer functionality was removed
  });
});
